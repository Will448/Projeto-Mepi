info:
  name: Me (usuário logado)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/me"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
