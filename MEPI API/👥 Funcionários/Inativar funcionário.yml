info:
  name: Inativar funcionário
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/funcionarios/1"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
